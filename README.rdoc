== Has Many Through

Demonstration of a common model relationship with rails - model A has many objects of model B through model C.  The classic example of this association provided in the rails documentation is "Doctors have many patients through appointments".

In terms of a relational database, the doctors table is assocated with the patients table through the appointments table, which contains fields for both both doctor ID and patient ID.
